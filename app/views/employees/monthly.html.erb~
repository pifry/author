<h1>Zestawienie miesięczne <%= t("date.month_names")[@date.month] %> <%= @date.year %></h1>

<table>
  <tr>
    <th>Nazwa</th>
    <th>Numer</th>
  </tr>

<% @employees.each do |employee| %>
  <tr>
    <td><%= employee.name %></td>
    <td><%= employee.number %></td>
  </tr>
	<tr>
		<td>
			<ol>
				<% employee.assignments.each do |asgn| %>
					<li><%= asgn.date %></li>
				<% end %>
			</ol>
		</td>
	</tr>
	<tr><td><%= link_to 'Dodaj', new_assignment_path(:employee_id => "3") %></td></tr>
<% end %>
</table>
<br />
